#include <stdio.h>

int runLength(int a[], int n, int i);

int main() {
    int n, k;
    int a[1000];
    int i;

    scanf("%d %d", &n, &k);

    for (i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }

    /* Use runLength to find and print the first
       session at which the rule is violated. */

    return 0;
}

int runLength(int a[], int n, int i) {
    /* Write your code here. */
}
